#!/bin/sh
set -eu
test -f "${TRIM_PKGETC}/app.env" || exec "$(dirname "$0")/install_callback"
mkdir -p "${TRIM_PKGVAR}/logs"
printf '%s [Hermes Agent] upgrade_callback\n' "$(date '+%Y-%m-%d %H:%M:%S%z')" >>"${TRIM_PKGVAR}/logs/install.log"
