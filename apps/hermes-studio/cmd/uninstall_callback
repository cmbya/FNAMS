#!/bin/sh
set -eu
mkdir -p "${TRIM_PKGVAR}/logs"
printf '%s [Hermes Studio] uninstall_callback\n' "$(date '+%Y-%m-%d %H:%M:%S%z')" >>"${TRIM_PKGVAR}/logs/install.log"
rm -f "${TRIM_PKGETC}/app.env" "${TRIM_PKGVAR}/hermes-studio.pid" "${TRIM_PKGVAR}/hermes-studio-log-viewer.pid"
